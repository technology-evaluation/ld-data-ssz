output csvw

map DIMENSIONEN from HDB_DIMENSIONEN {
	subject template propertyIri with Gruppencode;

	types
		rdf.Property

	properties
		schema.name from Gruppenname;
		schema.identifier from Gruppe;
}