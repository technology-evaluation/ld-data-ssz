template zeitIri "http://example.org/zeit/{0}-{1}-{2}"
template codeIri "https://ld.stadt-zuerich.ch/statistics/code/{0}"
template collectionIri "https://ld.stadt-zuerich.ch/statistics/collection/{0}"
template schemeIri "https://ld.stadt-zuerich.ch/statistics/scheme/{0}" 
template propertyIri "https://ld.stadt-zuerich.ch/statistics/property/{0}"
template measureIri "https://ld.stadt-zuerich.ch/statistics/measure/{0}" // TODO check if we can replace that with propertyIri or if we get clashes that way (BEW...)
template unitIri "https://ld.stadt-zuerich.ch/statistics/unit/{0}"