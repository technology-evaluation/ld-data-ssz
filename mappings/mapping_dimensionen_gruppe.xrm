output csvw

map DIMENSIONEN_GRUPPE_COLLECTION from HDB_DIMENSIONEN {
	subject template collectionIri with Dimension;

	types
		skos.Collection
		schema.DefinedTermSet

	properties
		schema.name from Dimensionname;
		skos.member template codeIri with Dimensioncode;
}